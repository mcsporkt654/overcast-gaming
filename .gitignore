node_modules/
.svelte-kit/
.claude/settings.local.json

# NOTE: build/ is intentionally committed — Render serves it directly
# (build cmd: `npm install`, start cmd: `npm start` -> node build/index.js).
# Do not gitignore build/ or deploys will run stale output.

*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.DS_Store
.env
.env.*
!.env.example
